// 12605번 : 단어 순서 뒤집기

#include <stdio.h>
#include <string.h>
#define size 26

int cnt = -1;
int i = 0;

int main()
{
    int n;
    scanf("%d", &n);
    fflush(stdin);
    while (n--)
    {
        char arr[size] = {
            0,
        };
        fgets(arr,size,stdin);
        
    }
    return 0;
}